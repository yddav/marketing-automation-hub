# UNTRAPD HUB POSTIZ CONFIGURATION
# Free social media automation with Postiz

# Application Settings
NODE_ENV=production
APP_NAME=UNTRAPD Hub Social Automation
APP_URL=http://localhost:3000

# Authentication (change this!)
NEXTAUTH_SECRET=untrapd_hub_social_automation_secret_change_this_in_production
NEXTAUTH_URL=http://localhost:3000

# Database Configuration
DATABASE_URL=postgresql://postiz:postiz_password@postgres:5432/postiz

# Redis Configuration
REDIS_URL=redis://redis:6379

# Email Configuration (optional - for notifications)
RESEND_API_KEY=your_resend_api_key_optional

# Upload Settings
UPLOAD_DIRECTORY=/app/uploads

# Social Media Platform Settings
# These will be configured through Postiz UI after startup

# UNTRAPD Hub Branding
BRAND_NAME=UNTRAPD Hub
BRAND_HANDLE=@untrapd.hub
BRAND_WEBSITE=https://hub.untrapd.com

# Instagram Settings (configured via OAuth in Postiz UI)
# INSTAGRAM_CLIENT_ID=configured_via_ui
# INSTAGRAM_CLIENT_SECRET=configured_via_ui

# Facebook Settings (configured via OAuth in Postiz UI)  
# FACEBOOK_APP_ID=configured_via_ui
# FACEBOOK_APP_SECRET=configured_via_ui

# Pinterest Settings (configured via OAuth in Postiz UI)
# PINTEREST_APP_ID=configured_via_ui
# PINTEREST_APP_SECRET=configured_via_ui